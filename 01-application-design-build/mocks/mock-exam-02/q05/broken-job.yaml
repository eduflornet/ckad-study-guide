apiVersion: batch/v1
kind: Job
metadata:
  name: broken-job
spec:
  template:
    spec:
      containers:
      - name: worker
        image: busybox:1.35
        command: ["sh", "-c", "exit 1"]
      restartPolicy: Always
  completions: 3
  parallelism: 2