apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-webapp.fullname" . }}
  labels:
    {{- include "secure-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secure-webapp.selectorLabels" . | nindent 4 }}
